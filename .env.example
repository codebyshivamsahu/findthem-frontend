# Copy to .env.local. NEVER commit real values.

# Backend API base URL
NEXT_PUBLIC_API_URL=http://localhost:5000

# Optional: face similarity service (face_server.py).
# Without it the sighting form still works, just with no photo comparison.
NEXT_PUBLIC_FACE_API_URL=http://localhost:5001
